services:
  - type: web
    name: yecy-cosmetic-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --worker-class gevent --workers 4 --bind 0.0.0.0:$PORT run:app"
    plan: free
    envVars:
      - key: FLASK_ENV
        value: production
