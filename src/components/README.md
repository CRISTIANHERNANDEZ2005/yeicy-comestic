# components/

Esta carpeta contiene los componentes divididos por roles:
- **admin/**: Componentes exclusivos del administrador.
- **cliente/**: Componentes exclusivos del cliente.
- **publico/**: Componentes para usuarios no autenticados.

Cada rol tiene sus propias subcarpetas `layout/` y `ui/` para layouts y elementos de UI específicos. 